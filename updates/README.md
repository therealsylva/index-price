# Update audits

Each successful `tools/update.py` run creates one immutable `YYYY-MM-DD`
directory here. It contains the declared batch, calculation receipt, output
hash manifest, match audit, change tables, and summary used to review that
publication. Licensed raw match payloads remain outside the repository.
